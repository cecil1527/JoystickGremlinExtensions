# these functions mirror the ones found in the JG repo's macro.py (at the time
# of 13.3 release)


def key_from_name(name):
    return name


def _send_key_down(key):
    # pass
    print(f'Key "{key}" down')


def _send_key_up(key):
    # pass
    print(f'Key "{key}" up')
